# SafeSpeak-
SafeSpeak uses AI where it adds value: language understanding, translation, moderation, safety detection, and matching. It does not attempt to diagnose users or replace professional healthcare.
